import { makeStyles } from '@material-ui/core/styles';

const stylesMapView = makeStyles((theme) => ({
    
    tableUsers: {
        viewBox:"0 -200 1500 1500",
      },

    container: {
    },

}));

  export default stylesMapView;